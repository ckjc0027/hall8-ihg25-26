# tournament-manager

This is a Vite + React + TypeScript project generated for you.
It contains your component (renamed to `App`) and is configured to deploy to GitHub Pages.

## Quick start

1. Install dependencies
```bash
npm install
```

2. Run development server
```bash
npm run dev
```

3. Deploy to GitHub Pages
- Create a GitHub repository named `tournament-manager` under your account `ckjc0027`.
- Push this project to that repo (main branch).
- Run:
```bash
npm run deploy
```
Your site will be published at: `https://ckjc0027.github.io/tournament-manager`

